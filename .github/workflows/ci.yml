name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  lint_and_test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      - name: Install Poetry
        run: |
          pip install poetry==2.3.4

      - name: Install dependencies
        run: poetry install --no-interaction --no-ansi

      - name: Run linting
        run: make lint
        env:
          # Minimal env so load_config() does not get None for required fields when tests import app modules
          SLACK_API_TOKEN: ""
          SLACK_CHANNEL_ID: ""
          SLACK_CHANNEL_NAME: ""
          ELASTICSEARCH_HOST: ""
          KIBANA_HOST: ""
          SELENIUM_HOST: ""

      - name: Run tests
        run: make test
        env:
          SLACK_API_TOKEN: ""
          SLACK_CHANNEL_ID: ""
          SLACK_CHANNEL_NAME: ""
          ELASTICSEARCH_HOST: ""
          KIBANA_HOST: ""
          SELENIUM_HOST: ""

      - name: Bot startup smoke check
        run: |
          poetry run python - <<'PY'
          from unittest.mock import MagicMock, patch

          from src.bot import kashiwaas
          from src.bot import kashiwaas_mattermost as mm_bot
          from src.utils.config import load_config

          with patch.object(kashiwaas, "App") as mock_app:
              cfg = load_config()
              app = kashiwaas.create_app(cfg)
              assert app is not None
              assert mock_app.called

          with patch.object(mm_bot, "Driver") as mock_driver:
              mock_d = MagicMock()
              mock_d.client.userid = "resolved-bot-user-id"
              mock_d.client.username = "mm_bot"
              mock_driver.return_value = mock_d
              _mm, d, _cc, _ts, _mmc, _bu = mm_bot.create_mattermost_stack(
                  load_config(
                      {
                          "MATTERMOST_URL": "https://mm.example.com",
                          "MATTERMOST_PAT": "pat",
                          "CURSOR_API_KEY": "k",
                      }
                  )
              )
              assert d is mock_d
              mock_d.login.assert_called_once()
          PY
        env:
          SLACK_BOT_TOKEN: "xoxb-dummy"
          CURSOR_API_KEY: "dummy"
          CURSOR_SOURCE_REPOSITORY: "https://github.com/takak2166/KashiwaaS"

  dry-run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Set up Docker Compose
        run: |
          cat << EOF > .env
          SLACK_CHANNEL_NAME=dummy-channel
          ELASTICSEARCH_HOST=http://elasticsearch:9200
          LOG_LEVEL=DEBUG
          KIBANA_HOST=http://kibana:5601
          SELENIUM_HOST=http://chrome:4444
          EOF
          docker compose up --build -d --wait
          docker compose cp .env app:/app/.env
          docker compose ps

      - name: Wait for services to be ready
        run: |
          set -e
          es_ready=0
          for i in {1..30}; do
            if docker compose exec -T app curl -fsS http://elasticsearch:9200/ >/dev/null 2>&1; then
              echo "Elasticsearch is available"
              es_ready=1
              break
            fi
            echo "Elasticsearch is unavailable - sleeping... $i"
            sleep 2
          done
          if [[ "$es_ready" -ne 1 ]]; then
            echo "Elasticsearch did not become ready in time"
            docker compose ps
            exit 1
          fi
          kbn_ready=0
          for i in {1..45}; do
            if docker compose exec -T app curl -fsS http://kibana:5601/api/status >/dev/null 2>&1; then
              echo "Kibana is available"
              kbn_ready=1
              break
            fi
            echo "Kibana is unavailable - sleeping... $i"
            sleep 5
          done
          if [[ "$kbn_ready" -ne 1 ]]; then
            echo "Kibana did not become ready in time"
            docker compose ps
            exit 1
          fi

      - name: Setup indices and dashboards
        run: |
          docker compose exec -T app poetry run python scripts/setup_indices.py --channel dummy-channel
          docker compose exec -T app poetry run python scripts/import_kibana_objects.py --overwrite

      - name: Run fetch command with dummy data
        run: docker compose exec -T app poetry run python -m src.cli fetch --dummy

      - name: Dry-run report command
        run: |
          docker compose exec -T app poetry run python -m src.cli report --type daily --channel dummy-channel --dry-run
          docker compose exec -T app poetry run python -m src.cli report --type weekly --channel dummy-channel --dry-run

      - name: Clean up
        if: always()
        run: |
          docker compose -f docker-compose.yml down --rmi all --volumes --remove-orphans
          rm .env

  notify-failure:
    environment: notification
    needs: [lint_and_test, dry-run]
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Notify Slack
        uses: slackapi/slack-github-action@v1.27.1
        with:
          channel-id: ${{ secrets.SLACK_CHANNEL_ID }}
          slack-message: |
            :x: CI Pipeline Failed
            *Repository:* ${{ github.repository }}
            *Workflow:* ${{ github.workflow }}
            *Branch:* ${{ github.ref }}
            *Commit:* ${{ github.sha }}
            *Failed Job:* ${{ needs.lint_and_test.result == 'failure' && 'lint_and_test' || 'dry-run' }}
            *View Details:* ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
